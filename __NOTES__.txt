
TODO

!!! mehr auf swing eingehen: was macht kotlin diesbzl besser
	- bildchen mit koma patienten als intro ;)
! fun ClientDbo?.toDomain() = if(null) null else Client(TXT_ID, TXT_NAME)
@ extension methods: MyImage.toReportRepresentation
! statt hasProperty => x?.getSomething ?: otherwise
- uebersicht: apply, let, with, ...
- neue libs anschauen: mockito-kotlin, hamkrest


-- website polishen
-- URL auf meetup seite geben
=========================================

>>>>>>>>>>>>>>>> ROADMAP GADSU DEMO <<<<<<<<<<<<<<<<

* create new client
	* enter name, select date
	* enter note and highlight bold
* create new treatment
	* date, time
	* select meridian
	* add dyn treatments
* behandlungsziel: preferences, new treatment
* protokoll, sammelprotokoll (reset of behandlungsziel)
* GCal
	* gcal creds und "gadsu_test" kalender eintragen
	* neuen termin erstellen, und gcal zeigen
	* in gcal neuen termin anlegen
	* "GCal sync" ausfuehren
* menu bar
	* bulk mail
	* acupuncture; show Bl1 in textfield
	* PDFs speichern


>>>>>>>>>>>>>>>> ROADMAP CODE DEMO <<<<<<<<<<<<<<<<

* enhance swing components:
	- fun JList.registerEnterPressed()
	- fun JTextComponent.enforceMaxCharacters()
	- fun simulationSaysLengthIsTooLong()

* how to build a swing UI with kotlin
* flyway java migration: V6_1__xprop_update
* UI test with spec4j

=========================================

// mehr fokus auf kotlin spezifika (weniger projekt generell krimskrams)
// intellij bookmarks verwenden fuer live herzeigen

* using kotlin since 0.6 (first gadsu was completely rewritten!)


Lessons Learned

* extension methods! (interop with java is mau; empty companion object in kotlin)
	- custom toXxx() methods: persistence, toMyImage()
	- custom validation in: fun Persistable.ensurePersisted(), String.isValidMail()
	-> lang.kt
	- date parse/format/conversion (util/joda/sql/gcal)
	- MyImage toXxxx converter
* more compact code
	- due to functional nature
	- no more lombok necessary
* collection API


GADSU FEATURES

* guice eventbus
* AopModule for logging
* TcmProps
* minors:
	- highlight text in text editor
	- textfields with suggestion

* generate acupunct based on ODS

